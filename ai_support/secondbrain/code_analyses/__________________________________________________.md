# Режим «стримера»: скрытие сохранений и личных путей в оверлеях.

```typescript
```typescript
interface GameMode {
  hideSaves: boolean;
  hidePaths: boolean;
}

const setGameMode = (mode: GameMode) => {
  // Реализация логики сохранения режима игры в localStorage или другом хранилище
};

const getGameMode = (): GameMode => {
  const storedMode = JSON.parse(localStorage.getItem('gameMode')) || { hideSaves: false, hidePaths: false };
  return storedMode;
};

// При инициализации приложения установить режим из localStorage
setGameMode(getGameMode());

// Функция для обновления режима игры и сохранения в localStorage
const toggleGameMode = (mode: GameMode) => {
  setGameMode(mode);
  setGameMode(getGameMode()); // Обновление состояния приложения
};

export { toggleGameMode };
```
```

Generated: 2026-06-22T06:09:12.574Z
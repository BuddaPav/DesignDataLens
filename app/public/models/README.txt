Chronos — активные 3D-ассеты (GLB/GLTF).

- По умолчанию сцена использует процедурную геометрию и CDN (jsDelivr) с запасным вариантом.
- Чтобы положить свою модель: добавьте `monolith.glb` сюда и при необходимости подключите загрузку в `ChronolithAnchor.tsx`.
